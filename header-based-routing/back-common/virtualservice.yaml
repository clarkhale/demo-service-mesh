apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: back
spec:
  hosts:
  - back
  http:
  - match:
    - headers: 
        middleversion:
          exact: v1
    route:
    - destination:
        host: back
        subset: v1v1
      weight: 100
    - destination:
        host: back
        subset: v1v2
      weight: 100
  - match:
    - headers: 
        middleversion:
          exact: v2
    route:
    - destination:
        host: back
        subset: v2v1
      weight: 100
    - destination:
        host: back
        subset: v2v2
      weight: 100
